import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://alanaaguirre.github.io",
  base: "/",
  output: "static", // Asegurar que Astro genera archivos estáticos
});